# Screenshots

Captured from the running app (Arabic UI, mobile viewport), regenerated with
`scripts/gen-shots.mjs`. The locator/maps render against OpenStreetMap in a real
browser; the mini-map tile is blank in headless capture (no external network).

| File | Screen |
|---|---|
| `home.png` | Home — the SOS button + complement-998 banner |
| `home-en.png` | Home in English (bilingual, LTR) |
| `onboarding-language.png` | Onboarding — language choice |
| `onboarding-role.png` | Onboarding — "I need help" / "I want to help" |
| `onboarding-permission.png` | Onboarding — why we ask for location |
| `onboarding-details.png` | Onboarding — name + phone |
| `sos-types.png` | Emergency type picker |
| `locator.png` | Locator card — Plus Code (`القوع`) + mini-map |
| `responder.png` | Responder — availability + nearby-requests empty state |
| `dashboard.png` | Coordinator dashboard — live metric tiles |

The verified WhatsApp dispatch screenshot lives separately at
`evidence/whatsapp-dispatch.png` (a real-phone capture; upload it there to
accompany `evidence/whatsapp-dispatch.md`).
